{% extends '@TeiEdition/Default/base.html.twig' %}

{% block title %}{{ 'New Organization'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'Add Organization'|trans }}</h1>

    {{ include('Admin/Organization/_form.html.twig') }}

    <div class="mt-4">
        <a class="btn btn-sm btn-light" href="{{ path('app_admin_organization_index') }}">
            {{ 'Back to overview'|trans }}
        </a>
    </div>
{% endblock %}
