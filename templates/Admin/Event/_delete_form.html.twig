<form style="display: inline-block" method="post" action="{{ path('app_admin_event_delete', {'id': event.id}) }}" onsubmit="return confirm('{% trans %}Are you sure that you want to delete this entry?{% endtrans %}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event.id) }}">
    <button class="btn btn-sm btn-light">{{ 'Delete'|trans }}</button>
</form>
